import styled from '@emotion/styled'
import { color, space, layout, flexbox, position, grid, background, border } from 'styled-system'

export const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${flexbox}
  ${position}
  ${grid}
  ${background}
  ${border}
`

